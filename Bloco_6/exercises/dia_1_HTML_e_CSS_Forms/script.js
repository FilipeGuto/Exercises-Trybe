function Curriculo() {
  const input = document.querySelectorAll('input').values;
  const button = document.querySelector('.function');
  button.addEventListener('click', (event) => {
    const evento = event.target;
    evento.preventDefault(values);
  })
}